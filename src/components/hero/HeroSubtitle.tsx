const HeroSubtitle = () => {
    return (
        <p className="text-base sm:text-lg md:text-xl text-gray-600 max-w-3xl mx-auto mb-10 px-4">
            Generate stunning AI images with end-to-end privacy, encrypted keys, and zero data retention
        </p>
    );
};

export default HeroSubtitle;
